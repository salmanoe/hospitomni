# HospitOmni application configuration (bootstrap module).
#
# @author Salman
# @version 1.0
# @since 2026-07-03

spring:
  application:
    name: hospitomni
  threads:
    virtual:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
